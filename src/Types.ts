// export interface ModalProps {
//   subscribe: (event: string, callback: (data: any) => void) => void;
//   publish: (event: string, data: any) => void;
// }

// apiProps: () => Promise<any[]>; // Assuming the promise resolves to an array
